# Dependencies
node_modules/

# Cloudflare Wrangler
.wrangler/
.dev.vars
.dev.vars.*

# Environment / secrets
.env
.env.*
!.env.example

# AUT-990: private-key material, by extension. Nothing of the sort has ever been committed
# here — history was scanned in full — and these lines are why it stays that way.
*.pem
*.p8
*.p12
*.jks
*.key

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
